/*
import { ComponentFixture, TestBed } from '@angular/core/testing';

import { TodasInspeccionesComponent } from './todas-inspecciones.component';

describe('TodasInspeccionesComponent', () => {
  let component: TodasInspeccionesComponent;
  let fixture: ComponentFixture<TodasInspeccionesComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      imports: [TodasInspeccionesComponent]
    })
    .compileComponents();

    fixture = TestBed.createComponent(TodasInspeccionesComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
 */
